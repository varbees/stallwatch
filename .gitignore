/target

# demo intermediates
demo/*.png

# generated by local release builds; CI produces its own per-release
/SHA256SUMS

# wrangler scratch; must never reach the served assets directory
.wrangler/
